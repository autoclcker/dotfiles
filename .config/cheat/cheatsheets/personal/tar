# Copy the folder data/ from current machine to remote server or system to folder /opt:
tar czf - data/ | ssh user@remoteserver "cd /opt && tar xvzf -"

# Copy the folder data/ in /opt to current machine from remote server or system:
ssh user@remoteserver "cd /opt && tar cfz - data/" | tar xfzv -
