[Commands]
1\Automatic=true
1\Command="copyq:\nvar tabName = 'buffer'\nvar minBytes = 3840\n\nfunction hasBigData() {\n    var itemSize = 0\n    var formats = dataFormats()\n    for (var i in formats) {\n        itemSize += data(formats[i]).size()\n        if (itemSize >= minBytes)\n            return true\n    }\n    return false\n}\n\nif (hasBigData()) {\n    setData(mimeOutputTab, tabName)\n}"
1\Icon=
1\Name=Big Data Tab
10\Command=copyq: plugins.itempinned.unpin()
10\Icon=
10\InMenu=true
10\Input=application/x-copyq-item-pinned
10\InternalId=copyq_pinned_unpin
10\Name=Unpin
10\Shortcut=p
11\Command=copyq: (plugins.itemtags.hasTag(decodeURIComponent('Important')) ? plugins.itemtags.untag : plugins.itemtags.tag)(decodeURIComponent('Important'))
11\Icon=
11\InMenu=true
11\InternalId=copyq_tags_tag:Important
11\Name=Toggle Tag “Important”
11\Shortcut=i
12\Command=copyq: plugins.itemtags.tag()
12\Icon=
12\InMenu=true
12\InternalId=copyq_tags_tag
12\Name=Add a Tag
12\Shortcut=t
13\Command=copyq: plugins.itemtags.untag()
13\Icon=
13\InMenu=true
13\Input=application/x-copyq-tags
13\InternalId=copyq_tags_untag
13\Name=Remove a Tag
13\Shortcut=shift+t
14\Command=copyq: plugins.itemtags.clearTags()
14\Icon=
14\InMenu=true
14\Input=application/x-copyq-tags
14\InternalId=copyq_tags_clear
14\Name=Clear all tags
14\Shortcut=ctrl+\\
15\Command="copyq:\nconst stack_tab = 'stack';\nconst source_tab = selectedtab();\nconst tab_mime = 'application/x-copyq-user-tab';\nconst index_mime = 'application/x-copyq-user-index';\n\nif (source_tab != stack_tab) {\n    const sel = ItemSelection().current();\n    const rows = sel.rows();\n    let pushed = sel.items().map((item, i) => {\n        item[tab_mime] = source_tab;\n        item[index_mime] = rows[i];\n        return item;\n    });\n\n    tab(stack_tab);\n    write(0, pushed);\n    tab(source_tab);\n    remove(rows);\n}\n"
15\Icon=
15\InMenu=true
15\Name=Push Item
15\Shortcut=s
16\Command="copyq:\nconst stack_tab = 'stack';\nconst source_tab = selectedtab();\n\nif (source_tab != stack_tab) {\n    tab('stack')\n    const item = getItem(0)\n    copy(item)\n    paste()\n    remove(0)\n}"
16\Icon=
16\InMenu=true
16\Name=Pop Item
16\Shortcut=shift+s
17\Command=copyq: toggle()
17\Icon=
17\InternalId=copyq_global_toggle
17\IsGlobalShortcut=true
17\Name=Show/hide main window
18\Command=copyq: showAt()
18\Icon=
18\InternalId=copyq_global_show_under_mouse
18\IsGlobalShortcut=true
18\Name=Show main window under mouse cursor
19\Command=copyq: ItemSelection(selectedTab()).selectRemovable().removeAll()
19\Icon=
19\InMenu=true
19\MatchCommand="copyq: tab(selectedTab()); if (size() == 0) fail()"
19\Name=Clear Current Tab
19\Shortcut=alt+l
2\Command=copyq:\nselectItems(currentItem() + 1)
2\Icon=
2\InMenu=true
2\Name=Next
2\Shortcut=ctrl+n
20\Automatic=true
20\Command="copyq:\nconst imageTab = 'images';\n\nfunction hasImageFormat(formats) {\n    for (const format of formats.values()) {\n        if (format.startsWith('image/'))\n            return true;\n    }\n    return false;\n}\n\nconst formats = dataFormats();\nif (hasImageFormat(formats)) {\n    setData(mimeOutputTab, imageTab);\n}"
20\Icon=
20\Name=Image Tab
21\Command="'use strict';\n\nconst timeFormat = 'yyyy-MM-dd hh:mm:ss';\nconst timeMatchRegExp = /^\\d{4}-\\d{2}-\\d{2} \\d{2}:\\d{2}:\\d{2}$/;\nconst mimeTags = 'application/x-copyq-tags';\n\nfunction setSelectionTime() {\n    const sel = ItemSelection().current();\n    if (sel.length !== 1) { return; }\n    \n    const tab_name = ItemSelection().tab;\n    if (tab_name == 'images') { return; }\n\n    const oldTags = sel.itemsFormat(mimeTags);\n    if (oldTags.length !== 1) { return; }\n\n    let tags = str(oldTags[0] || '').split(',').map(x => x.trim());\n    const time = dateString(timeFormat);\n    if (tags.includes(time)) { return; }\n\n    tags = tags.filter(x => x !== '' && !timeMatchRegExp.test(x));\n    tags.push(time);\n    sel.setItemsFormat(mimeTags, tags.join(', '));\n}\n\nconst onItemsAdded_ = onItemsAdded;\nonItemsAdded = function() {\n    setSelectionTime();\n    return onItemsAdded_();\n};\n\nconst onItemsChanged_ = onItemsChanged;\nonItemsChanged = function() {\n    setSelectionTime();\n    return onItemsChanged_();\n};"
21\Icon=
21\IsScript=true
21\Name=Show Copy Time
3\Command=copyq:\nselectItems(currentItem() - 1)
3\Icon=
3\InMenu=true
3\Name=Previous
3\Shortcut=ctrl+p
4\Command="copyq:\nvar shortcut = str(data(mimeShortcut))\nselectItems(shortcut)\ncopy(mimeItems, pack(getItem(shortcut)))\nhide()\npaste()"
4\Icon=
4\InMenu=true
4\Name=Select and Paste Nth Item
4\Shortcut=1, 2, 3, 4, 5, 6, 7, 8, 9, 0
5\Command="copyq:\nif ( dataFormats().indexOf(mimeHidden) != -1 )\n removeData(mimeHidden)\nelse\n setData(mimeHidden, 1)"
5\Icon=
5\InMenu=true
5\Name=Hide/Show Item Content
5\Shortcut=x
6\Automatic=true
6\Command="copyq:\nvar tabName = '&URLs';\nfunction lower(data) {\n    return str(data).toLowerCase()\n}\n\nfunction findHeader(reply, headerName) {\n    reply.data  // fetches data and headers\n    var headers = reply.headers\n    for (var i in headers) {\n        var header = headers[i]\n        if (lower(header[0]) === headerName)\n           return header[1]\n    }\n    return ''\n}\n\nfunction fetchContent(url, maxRedirects) {\n    if (maxRedirects === undefined)\n        maxRedirects = 4\n\n    serverLog('Fetching: ' + url)\n    var reply = networkGet(url)\n    if (maxRedirects == 0)\n        return reply\n\n    var header = findHeader(reply, 'location')\n    if (header)\n        return fetchContent(header, maxRedirects - 1)\n\n    return reply\n}\n\nfunction decodeHtml(html) {\n    return html.replace(/&#(\\d+);/g, function(match, charCode) {\n        return String.fromCharCode(charCode);\n    });\n}\n\nfunction isHtml(reply) {\n    var headers = reply.headers\n    for (var i in headers) {\n      var header = headers[i]\n      if (lower(header[0]) === 'content-type')\n         return lower(header[1]).indexOf(mimeHtml) === 0\n    }\n    return false\n}\n\nfunction grep(content, re) {\n    return content ? (re.exec(content) || [])[1] : ''\n}\n\nfunction getTitle(content) {\n    var title = grep(content, /<title[^>]*>([^<]*)<\\/title>/i)\n    return title ? decodeHtml(title.trim()) : ''\n}\n\nfunction getFavicon(content) {\n    var iconLine = grep(content, /<link([^>]*rel=[\"'](?:shortcut )?icon[\"'][^>]*)/i)\n    var icon = grep(iconLine, /href=[\"']([^\"']*)/i)\n\n    if (!icon)\n      return ''\n\n    // Icon path can be complete URL.\n    if (icon.indexOf('://') != -1)\n      return fetchContent(icon).data\n\n    // Icon path can be missing protocol.\n    if (icon.substr(0, 2) === '//') {\n      var i = url.search(/\\/\\//)\n      var protocol = (i == -1) ? 'http:' : url.substr(0, i)\n      return fetchContent(protocol + icon).data\n    }\n\n    // Icon path can be relative to host URL.\n    if (icon[0] === '/') {\n      var baseUrl = url.substr(0, url.search(/[^\\/:](\\/|$)/) + 1)\n      return fetchContent(baseUrl + icon).data\n    }\n\n    // Icon path can be relative to current URL.\n    var baseUrl = url.substr(0, url.lastIndexOf('/') + 1)\n    return fetchContent(baseUrl + icon).data\n}\n\nvar url = str(input()).trim()\nserverLog('Fetching icon and title: ' + url)\n\n// URL already added? (Just check the top of the list.)\nif (url === str(read(0)))\n    abort()\n\n// Fetch HTML.\nvar reply = fetchContent(url)\nif (!isHtml(reply))\n    abort()\n\nvar content = str(reply.data)\n\nvar title = getTitle(content)\nvar icon = getFavicon(content)\n\nsetData(mimeText, url)\nsetData(mimeItemNotes, title || '')\nsetData(mimeIcon, icon)\nsetData(mimeOutputTab, tabName)"
6\Icon=
6\Input=text/plain
6\Match=^https?://
6\Name=Tab for URLs with Title and Icon
7\Command="copyq:\nvar color = 'rgba(0, 255, 0, 0.5)'\nvar currentColor = str(selectedItemData(0)[mimeColor]);\nif (currentColor != color) {\n setData(mimeColor, color)\n} else {\n removeData(mimeColor)\n}"
7\Icon=
7\InMenu=true
7\MatchCommand="copyq:\nvar color = 'rgba(0, 255, 0, 0.5)'\nvar currentColor = str(selectedItemData(0)[mimeColor])\nif (currentColor != color) {\n menuItem['text'] = 'Mark Items'\n menuItem['tag'] = '__'\n menuItem['color'] = color.replace(/\\d+\\.\\d+/, 1)\n} else {\n menuItem['text'] = 'Unmark Items'\n menuItem['tag'] = 'x'\n menuItem['color'] = 'white'\n}\nmenuItem['icon'] = ''"
7\Name=Mark/Unmark Items
7\Shortcut=m
8\Command=copyq open %1
8\HideWindow=true
8\Icon=
8\InMenu=true
8\Match=^(https?|ftps?|file)://
8\Name=Open in &Browser
8\Shortcut=o
9\Command=copyq: plugins.itempinned.pin()
9\Icon=
9\InMenu=true
9\Input=!OUTPUT
9\InternalId=copyq_pinned_pin
9\Name=Pin
9\Output=application/x-copyq-item-pinned
9\Shortcut=p
size=21
